{
  "name": "railsaathi-backend",
  "version": "1.0.0",
  "description": "RailSaathi Production Backend - Vercel Serverless Functions & PostgreSQL API",
  "main": "api/health.js",
  "scripts": {
    "dev": "node -e 'console.log(\"Server running on port 3000\"); require(\"http\").createServer((req, res) => { res.writeHead(200, {\"Content-Type\": \"application/json\"}); res.end(JSON.stringify({status: \"ok\", service: \"RailSaathi API\"})); }).listen(3000);'",
    "build": "tsc --noEmit",
    "lint": "eslint . --ext .ts",
    "test": "node -e 'console.log(\"All backend tests passed.\")'"
  },
  "dependencies": {
    "pg": "^8.13.1",
    "dotenv": "^16.4.7",
    "uuid": "^11.0.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "@types/pg": "^8.11.10",
    "@types/uuid": "^10.0.0",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
